{
  "name": "knowledge_graph",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "budo index.js:bundle.js --live --verbose --open",
    "build": "browserify index.js -t es2040 > docs/bundle.js && cp index.html docs/index.html && cp index.css docs/index.css"
  },
  "author": "jayrbolton",
  "license": "MIT",
  "devDependencies": {
    "budo": "^11.5.0",
    "es2040": "^1.2.6",
    "standard": "^12.0.1"
  },
  "dependencies": {
    "form-serialize": "^0.7.2",
    "snabbdom": "^0.7.3"
  }
}
